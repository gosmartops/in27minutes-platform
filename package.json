{
  "name": "in27minutes-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*",
    "api/*",
    "workers/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web"
  }
}
